# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'chiff' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  # Pods for chiff
  pod 'Sodium', '~> 0.9'

  target 'chiffTests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'chiffCredentialProvider' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  # Pods for chiffCredentialProvider
  pod 'Sodium', '~> 0.9'
end

target 'chiffNotificationExtension' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for chiffNotificationExtension
  pod 'Sodium', '~> 0.9'

end
